# Reference only: the app does not automatically load this file.
# Default: an absolute SQLite path to backend/boardlet.db, independent of cwd.
# To use a custom location, export before starting uvicorn:
# export DATABASE_URL='sqlite:////absolute/path/to/boardlet.db'
# export BOARDLET_STORE='sqlalchemy'
# To reproduce the earlier in-memory implementation:
# export BOARDLET_STORE='memory'
